/**
 * Simulados para alunos B2B.
 * Re-usa o mesmo componente do portal padrão.
 * O branding (cores, logo) vem do layout pai /partners/[slug]/student/layout.tsx.
 */
export { default } from '@/app/portal/student/simulado/page';
